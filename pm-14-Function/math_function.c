#include<stdio.h>
#include<string.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    /*
    double x;
    scanf("%lf",&x);
    int ans = ceil(x);
    printf("%d",ans);
    return 0;
    */

   /*
    double x;
    scanf("%lf",&x);
    int ans = floor(x);
    printf("%d",ans);
    return 0;
    */

   /*
    double x;
    scanf("%lf",&x);
    int ans = round(x);
    printf("%d",ans);
    return 0;
    */

    /*
    int x;
    scanf("%d",&x);
    double ans = sqrt(x);
    printf("%0.2lf",ans);
    return 0;
    */

   /*
    int x, y;
    scanf("%d %d",&x,&y);
    int ans = pow(x, y);
    // int ans = pow(x*1.0, y*1.0);
    printf("%d",ans);
    return 0;
    */

   /*
    double x, y;
    scanf("%lf %lf",&x,&y);
    double ans = pow(x, y);
    printf("%0.2lf",ans);
    return 0;
    */

    int x;
    scanf("%d",&x);
    int ans = abs(x);
    printf("%d",ans);
    return 0;

}